<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
       xmlns:views="clr-namespace:StampCatalog"
       xmlns:views4="clr-namespace:StampCatalog.Views"
       FlyoutHeaderBehavior="CollapseOnScroll"
       x:Class="StampCatalog.AppShell">

    <Shell.FlyoutHeaderTemplate>
        <DataTemplate>
            <Grid BackgroundColor="Black"
                  HeightRequest="200">
            <Image Aspect="AspectFill"
                   Source="FlyoutHeader.jpg"
                   Opacity="0.6" />
            </Grid>
        </DataTemplate>
    </Shell.FlyoutHeaderTemplate>

    <ShellContent Title="DOMOV"
                  Icon="home.png"
                  ContentTemplate="{DataTemplate views:MainPage}"/>


    <FlyoutItem Title="SLOVENSKÁ REPUBLIKA"
                Icon="slovak.png">
        <Tab>
            <ShellContent Title="Test">
                <views4:StampsView/>
            </ShellContent>
        </Tab>
    </FlyoutItem>

    <FlyoutItem Title="ČESKÁ REPUBLIKA"
                Icon="czechoslovak.png" >
        <Tab>
        </Tab>
    </FlyoutItem>

    <FlyoutItem Title="ČESKOSLOVENSKO"
                Icon="czechoslovak.png" >
        <Tab>
        </Tab>
    </FlyoutItem>
</Shell>